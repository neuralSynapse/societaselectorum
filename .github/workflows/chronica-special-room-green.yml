name: CHRONICA Special Room Generation GREEN

on:
  workflow_dispatch:
  push:
    branches: [fix/chronica-runtime-qa]
    paths: ['.github/workflows/chronica-special-room-green.yml']

permissions:
  contents: read

jobs:
  green:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/chronica-runtime-qa
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -q pytest
      - run: pytest -q game/chronica-harun/tests/test_special_room_generation_runtime_gate.py
