ead">Uma entrada direta para quem deseja organizar a pergunta e receber uma direção oracular inicial por ${fmt(state.product.price)}.</p><div class="summary"><div class="summary-row"><span>Acesso ao Hórus IA</span><b>${fmt(state.product.price)}</b></div><div class="summary-row"><span>Total</span><b>${fmt(state.product.price)}</b></div></div><div class="secure-box"><b>Pagamento seguro</b><br>Ao continuar, você será direcionado ao checkout do Hórus IA para concluir por Pix, cartão ou pelos métodos disponíveis.</div><div class="payment-badges"><span>PIX</span><span>CARTÃO</span><span>CHECKOUT STRIPE</span></div><button class="btn btn-primary" onclick="completePayment()">IR PARA O PAGAMENTO SEGURO</button><p class="micro center">A confirmação e a entrega serão liberadas após a aprovação do pagamento.</p></div>`}else{const pdf=state.product.pdf,extra=state.product.extra;h=`<div class="content checkout"><div class="eyebrow">Checkout seguro</div><h2>${state.product.name}</h2><p class="lead">Proteja a clareza que você acabou de construir. Estes complementos ajudam a conservar o plano e esclarecer o ponto que ainda pode influenciar sua decisão.</p><div class="summary"><div class="summary-row"><span>Plano escolhido</span><b>${fmt(state.product.price)}</b></div>${state.bumps.map(b=>`<div class="summary-row"><span>${b==='pdf'?'Dossiê Oracular em PDF':'Pergunta extra de confirmação'}</span><b>${fmt(bumpPrice(b))}</b></div>`).join('')}<div class="summary-row"><span>Total desta seleção</span><b>${fmt(total())}</b></div></div><div class="bump-list"><label class="bump bump-conversion"><input type="checkbox" ${state.bumps.includes('pdf')?'checked':''} onchange="toggleBump('pdf')"><span><b>Não deixe sua leitura se perder quando a rotina voltar</b><small>Receba um Dossiê Oracular em PDF com pergunta, cartas, síntese, Conselho do Oráculo e plano de próximos movimentos para revisar e aplicar.</small><span class="bump-benefits"><em>clareza registrada</em><em>consulta organizada</em><em>plano sempre acessível</em></span></span><span class="bump-price">${fmt(pdf)}</span></label><label class="bump"><input type="checkbox" ${state.bumps.includes('extra')?'checked':''} onchange="toggleBump('extra')"><span><b>Esclareça o ponto que ainda pode mudar sua decisão</b><small>Acrescente uma pergunta objetiva de confirmação conectada ao tema central da leitura.</small><span class="bump-benefits"><em>mais precisão</em><em>menos dúvida</em><em>uma questão adicional</em></span></span><span class="bump-price">${fmt(extra)}</span></label></div><div class="secure-box"><b>Pagamento seguro</b><br>Ao continuar, você será direcionado ao checkout correspondente ao plano escolhido. O valor final será recalculado com os complementos selecionados, e o pagamento poderá ser concluído por Pix, cartão ou pelos métodos disponíveis.</div><div class="payment-badges"><span>PIX</span><span>CARTÃO</span><span>CHECKOUT STRIPE</span></div><button class="btn btn-primary" onclick="completePayment()">IR PARA O PAGAMENTO SEGURO</button><p class="micro center">A confirmação e a entrega serão liberadas após a aprovação do pagamento.</p></div>`}break}
case 27:{if(state.product.id==='horusai'){h=`<div class="content center"><div class="eyebrow">Acesso inicial</div><h2>Seu caminho de entrada foi definido.</h2><p class="lead center">Após a confirmação do pagamento, você receberá acesso ao Hórus IA para registrar uma pergunta e receber uma orientação oracular inicial.</p><div class="final-box"><h3>Resumo da sua escolha</h3><p><b>Produto:</b> Consulta Inicial com Hórus IA</p><p><b>Valor:</b> ${fmt(state.product.price)}</p><p><b>Entrega:</b> pergunta, leitura inicial, síntese do padrão e próximo passo.</p></div><button class="btn btn-primary" onclick="reset()">REINICIAR A EXPERIÊNCIA</button></div>`}else if(state.postStage==='magnet'){h=`<div class="content"><div class="post-progress"><span class="active"></span><span></span><span></span></div><div class="post-offer"><div class="eyebrow">Continuidade mental e emocional</div><h2>Não deixe a clareza da leitura desaparecer quando os padrões antigos voltarem.</h2><p class="lead">O Ímã de Dinheiro reúne sete áudios de hipnose e reprogramação mental para trabalhar identidade, receptividade, preservação, foco e ação material.</p><div class="price">${fmt(9.97)}</div><div class="ritual-note"><b>Forja simbólica:</b> a edição foi preparada na quinta-feira, na primeira hora planetária após o nascer do sol, sob a regência de Júpiter e em fase crescente, e selada no domingo, na primeira hora após o nascer do sol, sob a regência do Sol. A estrutura combina expansão joviana e direção solar como símbolos de intenção, disciplina e construção.</div><div class="disclaimer">Hipnose e prática ritual não garantem dinheiro. A ferramenta busca fortalecer estados internos e comportamentos compatíveis com construção financeira.</div><div class="post-choice"><button class="btn btn-primary" onclick="postDecision('magnet',true)">ADICIONAR ÍMÃ DE DINHEIRO · ${fmt(9.97)}</button><button class="btn btn-ghost" onclick="postDecision('magnet',false)">SEGUIR SEM OS ÁUDIOS</button></div></div></div>`}else if(state.postStage==='numerology'){h=`<div class="content"><div class="post-progress"><span class="active"></span><span class="active"></span><span></span></div><div class="post-offer"><div class="eyebrow">Aprofundamento numerológico</div><h2>Veja os ciclos e padrões numéricos que atravessam sua construção material.</h2><p class="lead">O Mapa Numerológico Financeiro organiza números do nome e nascimento, ciclos, tensões, potenciais materiais e períodos simbólicos em uma leitura completa.</p><div class="price">${fmt(147)}</div><ul><li>códigos simbólicos do nome e nascimento</li><li>ciclos e tendências materiais</li><li>forças, tensões e pontos de atenção</li><li>direção prática para o período</li></ul><div class="post-choice"><button class="btn btn-primary" onclick="postDecision('numerology',true)">QUERO MEU MAPA FINANCEIRO · ${fmt(147)}</button><button class="btn btn-ghost" onclick="postDecision('numerology',false)">CONCLUIR SEM O MAPA</button></div></div></div>`}else{h=`<div class="content"><div class="post-progress"><span class="active"></span><span class="active"></span><span class="active"></span></div><div class="final-box"><h2>Seu percurso foi concluído.</h2><p><b>Consulta:</b> ${state.product.name} · ${fmt(state.product.price)}</p><p><b>Complementos:</b> ${state.bumps.length?state.bumps.map(b=>b==='pdf'?'Dossiê Oracular em PDF':'Pergunta extra').join(', '):'nenhum'}</p><p><b>Continuidade:</b> ${state.upsells.length?state.upsells.map(u=>u==='magnet'?'Ímã de Dinheiro':'Mapa Numerológico Financeiro').join(', '):'nenhuma expansão adicionada'}</p><p>Após a confirmação do pagamento, você receberá as instruções de entrega e, quando aplicável, o agendamento.</p></div><button class="btn btn-primary" onclick="reset()">REINICIAR A EXPERIÊNCIA</button></div>`}break}
}
app.innerHTML=h;}
window.next=next;window.showDownsell=showDownsell;window.closeDownsell=closeDownsell;window.selectHorusAI=selectHorusAI;window.answer=answer;window.chooseCard=chooseCard;window.selectProduct=selectProduct;window.toggleBump=toggleBump;window.choosePayment=choosePayment;window.completePayment=completePayment;window.toggleUpsell=toggleUpsell;window.postDecision=postDecision;window.saveLead=saveLead;window.reset=reset;window.jump=jump;
await Promise.all([hydrateAssets(),loadCheckoutConfig()]);restore();ensureSpread();const qp=new URLSearchParams(location.search);if(qp.has('step')){jump(qp.get('step'));if(qp.get('downsell')==='1'){state.downsell=true;render()}}if(CONFIG.review){document.getElementById('review').style.display='flex';const sel=document.getElementById('stepSelect');sel.innerHTML=STEPS.map((x,i)=>`<option value="${i}">${i+1}. ${x}</option>`).join('');document.getElementById('jumpBtn').onclick=()=>jump(sel.value);document.getElementById('resetBtn').onclick=reset}render();
})();
</script>
</body>
</html>