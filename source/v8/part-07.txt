iv><div class="method"><span>03 · PORTAL</span><h3>Imagem operativa</h3><p>Na tradição hermética, cada carta pode servir como portal simbólico para contemplação, meditação, estudo e trabalho ritual.</p></div></div><div class="notice">Uma carta isolada não produz uma sentença. A leitura nasce da pergunta, da posição, das relações e da interpretação do conjunto.</div><button class="btn btn-primary" onclick="next()">DEFINIR O QUE PRECISO DA LEITURA</button></div>`;break;
case 17:h=`<div class="content">${qScreen('objection','Antes de avançar')}</div>`;break;
case 18:h=`<div class="content">${qScreen('format','Nível de profundidade',`Seu percurso aponta para <b>${p.short.toLowerCase()}</b>.`)}</div>`;break;
case 19:h=`<div class="content"><div class="eyebrow">Dados da consulta</div><h2>Agora organize sua questão principal.</h2><p class="lead">Esses dados permitem relacionar sua pergunta ao percurso e preparar a entrega adequada.</p><div class="form-grid"><div class="field"><label>Nome completo</label><input id="name" value="${esc(state.lead.name||'')}"></div><div class="field"><label>Data de nascimento</label><input id="birth" type="date" value="${esc(state.lead.birth||'')}"></div><div class="field"><label>Hora de nascimento, se souber</label><input id="time" type="time" value="${esc(state.lead.time||'')}"></div><div class="field full"><label>Qual é a questão financeira que mais exige clareza?</label><textarea id="question" placeholder="Descreva a situação, a decisão ou o ciclo que deseja compreender.">${esc(state.lead.question||'')}</textarea></div></div><button class="btn btn-primary" onclick="saveLead()">PROCESSAR MEU DIAGNÓSTICO</button><p class="micro">Na implementação pública, esta etapa dependerá de política de privacidade e canal seguro de armazenamento.</p></div>`;break;
case 20:h=`<div class="content processing"><div><div class="orbit"></div><div class="eyebrow">Relacionando respostas, carta e contexto</div><h2>Seu diagnóstico está sendo organizado.</h2><p class="lead center">A simulação cruza os eixos do percurso para montar uma leitura coerente.</p><button class="btn btn-primary" onclick="next()">VER MEU DIAGNÓSTICO</button></div></div>`;break;
case 21:h=`<div class="content"><div class="eyebrow">Diagnóstico adaptativo</div><h2>${p.name}</h2><div class="profile-grid"><div class="profile-card active"><span class="tag">Eixo dominante</span><h3>${p.short}</h3><p>${p.truth}</p></div><div class="profile-card"><span class="tag">Sombra</span><h3>O que mantém o padrão</h3><p>${p.shadow}</p></div><div class="profile-card"><span class="tag">Abertura</span><h3>O primeiro deslocamento</h3><p>Seu percurso pede ${p.move}.</p></div></div><div class="insight"><h3>Síntese com ${state.card.name}</h3><p>${state.card.text} O trabalho agora é transformar essa percepção em um movimento verificável.</p></div><button class="btn btn-primary" onclick="next()">RECEBER O CONSELHO DO ORÁCULO</button></div>`;break;
case 22:h=`<div class="content center"><div class="eyebrow">Conselho do Oráculo</div><h2>${state.card.advice}</h2><p class="lead center">Este conselho não decide por você. Ele define o ponto de consciência que merece governar a próxima escolha.</p><div class="notice"><b>Ponto de virada:</b> ${p.move}.</div><button class="btn btn-primary" onclick="next()">TRANSFORMAR ISSO EM PLANO</button></div>`;break;
case 23:h=`<div class="content"><div class="eyebrow">Plano inicial do seu percurso</div><h2>O diagnóstico precisa evoluir para cartas, passos e verificação.</h2><div class="action-preview"><div class="action-item"><span>01</span><div><b>Carta do padrão</b><p>${p.shadow}</p></div></div><div class="action-item"><span>02</span><div><b>Carta do próximo movimento</b><p>${state.card.advice}</p></div></div><div class="action-item"><span>03</span><div><b>Carta de consolidação</b><p>Transformar ${p.move} em uma ação com prazo, critério e indicador de retorno.</p></div></div></div><div class="notice"><b>Na consulta paga:</b> o plano deixa de ser genérico. Novas cartas são abertas para investigar raiz, bloqueio, recurso, ação e tendência conforme a profundidade escolhida.</div><button class="btn btn-primary" onclick="next()">VER COMO O PLANO É APROFUNDADO</button></div>`;break;
case 24:h=`<div class="content"><div class="eyebrow">Relatos reais · continuidade</div><h2>Veja experiências que se aproximam da direção revelada no seu percurso.</h2><p class="lead">Clareza ganha valor quando se transforma em movimento, reorganização e construção.</p><div class="testimonial-grid">${proofHtml('second')}</div><div class="notice">Os relatos representam experiências individuais. O que cada pessoa constrói depende de suas escolhas e ações.</div><button class="btn btn-primary" onclick="next()">VER MEU PLANO E OS VALORES</button></div>`;break;
case 25:h=`<div class="content"><div class="eyebrow">Planos de ação oracular</div><h2>Escolha até onde a leitura precisa avançar.</h2><p class="lead">Cada formato inclui novas cartas e próximos passos. O preço aparece somente depois de você compreender o plano.</p><div class="action-preview offer-action"><div class="action-item"><span>01</span><div><b>Interromper o padrão</b><p>${p.shadow}</p></div></div><div class="action-item"><span>02</span><div><b>Executar o próximo movimento</b><p>${state.card.advice}</p></div></div><div class="action-item"><span>03</span><div><b>Consolidar e revisar</b><p>Transformar ${p.move} em ação com prazo, evidência e retorno.</p></div></div></div><div class="notice"><b>Plano recomendado:</b> ${rec==='complete'?'Consulta Financeira Completa com 9 cartas':rec==='deep'?'Leitura Financeira Profunda com 5 cartas':'Leitura Financeira Essencial com 3 cartas'}.</div><div class="offer-grid">${Object.values(PRODUCTS).filter(x=>x.id!=='horusai').map(x=>`<div class="offer ${rec===x.id?'recommended':''}">${rec===x.id?'<span class="recommended-flag">RECOMENDADO PELO SEU PERCURSO</span>':''}<span class="tag">${x.id==='essential'?'1 PERGUNTA':x.id==='deep'?'ATÉ 3 PERGUNTAS':'CONSULTA COMPLETA'}</span><div class="spread-badge">${x.spread}</div><h3>${x.name}</h3><div class="price">${fmt(x.price)}</div><p>${x.desc}</p><ul>${x.items.map(i=>`<li>${i}</li>`).join('')}</ul>${x.id==='complete'?'<div class="included"><b>Incluído:</b> áudio de integração criado especificamente a partir da consulta e do plano definido.</div>':''}<button class="btn ${rec===x.id?'btn-primary':'btn-secondary'}" onclick="selectProduct('${x.id}')">ESCOLHER ESTE PLANO · ${fmt(x.price)}</button></div>`).join('')}</div><div class="decline-wrap"><button class="decline-link" onclick="showDownsell()">Ainda não quero uma consulta conduzida por Frater Hórus L93</button></div><p class="micro center">Nenhum formato garante resultado financeiro. O valor corresponde ao escopo, às cartas abertas, ao tempo e à profundidade da entrega.</p>${state.downsell?`<div class="downsell-backdrop" role="dialog" aria-modal="true"><div class="downsell-card"><button class="downsell-close" onclick="closeDownsell()" aria-label="Fechar">×</button><div class="eyebrow">Alternativa de entrada</div><h2>Comece com uma orientação mais simples sem abandonar a questão que trouxe você até aqui.</h2><p class="lead">O Hórus IA organiza sua pergunta, relaciona o contexto ao sistema oracular e entrega uma direção inicial para o próximo movimento.</p><div class="downsell-price">${fmt(PRODUCTS.horusai.price)}</div><div class="downsell-list"><div>Uma pergunta objetiva</div><div>Leitura oracular digital inicial</div><div>Síntese do padrão e próximo passo</div></div><button class="btn btn-primary" onclick="selectHorusAI()">CONSULTAR O HÓRUS IA · ${fmt(PRODUCTS.horusai.price)}</button><p class="micro center">Esta alternativa é mais simples e não substitui uma consulta conduzida por Frater Hórus L93.</p></div></div>`:''}</div>`;break;
case 26:{if(state.product.id==='horusai'){h=`<div class="content checkout"><div class="eyebrow">Checkout seguro</div><h2>Consulta Inicial com Hórus IA</h2><p class="l