# HĀRŪN Survivor Rebuild Implementation Plan

Goal: create the canonical `/harun-survivor.html` mobile game with original procedural/vector characters, richer VFX, boss warnings, animated upgrade reveals, persistent campaign/meta progression, and no dependency on character PNGs from the other Hārūn games.

Execution order: test and add the procedural art module; test and add campaign/meta-state helpers; integrate both into a new canonical survivor runtime; verify the canonical page contains no references to the old imported character PNGs; publish to WebsitePublisher without changing the 2D roguelite or FPS runtimes.